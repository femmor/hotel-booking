let express,readdirSync,cors,mongoose;_616‍.w("express",[["default",["express"],function(v){express=v}]]);_616‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_616‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_616‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

require("dotenv").config({ path: "./.env" });



const morgan = require("morgan");

const app = express();

// Database connection
const options = {
  useNewUrlParser: true,
  useUnifiedTopology: true,
};

mongoose
  .connect(process.env.DATABASE_URI, options)
  .then(() => console.log("Database connection established successfully..."))
  .catch(err => _616‍.g.console.log(err.message));

// middlewares
app.use(cors());
app.use(morgan("dev"));
app.use(express.json());

// route middleware
readdirSync("./routes").map(r => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 3005;

app.listen(port, () => {
  console.log(`Server started on port ${port}`);
});
